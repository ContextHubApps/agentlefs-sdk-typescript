# Generated SDK output. Regenerated from the API repo's openapi/v1.yaml by
# `fern generate --local`, so committing it would mean reviewing a 122-file
# machine-written diff on every spec change.
#
# This is a deliberate choice for the CURRENT stage, where nothing is published. Once
# the package ships to npm, the generated tree has to be committed (or built in CI),
# because a consumer installing from git needs the code to exist. Revisit then.
sdks/

# Fern working files
**/.preview
**/.definition
node_modules/
